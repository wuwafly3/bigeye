{
  "name": "aether-gazer-archive",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "深空之眼档案馆 — 非官方粉丝存档站 (Aether Gazer Archive)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cf:dev": "npm run build && wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "^0.170.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "harmonyos-sans-sc-webfont-splitted": "^1.1.0"
  },
  "devDependencies": {
    "playwright-core": "^1.62.0",
    "vite": "^5.4.0",
    "wrangler": "^4.118.0"
  }
}
